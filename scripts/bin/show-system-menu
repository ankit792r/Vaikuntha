#!/bin/sh


choice=$(printf "🔏 Lock\n🚪 Logout\n🔃 Reboot\n🔒 Shutdown" \
    | fuzzel --dmenu --prompt="System » ")

case "$choice" in
    "🔏 Lock")
        swaylock
        ;;
    "🚪 Logout")
        hyprctl dispatch exit
        ;;
    "🔃 Reboot")
        systemctl reboot
        ;;
    "🔒 Shutdown")
        systemctl poweroff
        ;;
esac
